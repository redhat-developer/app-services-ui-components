import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="Intro" />

# app-services-ui-components

app-services-ui-components contains a number of shared components for the UI. 
The UIs are a set of micro-frontends, implemented using Webpack module federation.

These are visual components with behavior.